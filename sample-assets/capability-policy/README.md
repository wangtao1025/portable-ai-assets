# Sample Capability Policy Baseline

This public-safe baseline demonstrates how capability policy preview compares reviewed capability state against current team/project pack declarations.

It contains fake example capability names only. It does not contain credentials, webhook URLs, provider identifiers, tenant IDs, runtime execution logs, or private project details.
