# Sample Adapters

This directory contains public-safe adapter contract examples.

Suggested use:
- inspect adapter contract shape
- validate sample contracts with `--validate-schemas`
- inspect connector inventory with `--connectors`
- copy the example as a starting point for a new third-party runtime adapter
